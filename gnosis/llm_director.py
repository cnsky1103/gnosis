# ==========================================
# Pass 1: 角色提取 (Casting Director)
# ==========================================
PASS1_PROMPT_TEMPLATE = """
你是一位专业的有声书选角导演。你的任务是阅读小说片段，并从中提取出**新登场**的实体角色。

# 当前已知角色 (Known Characters)
{known_characters_str}
(如果为空，说明这是小说的开头)

# 核心提取任务
1. 找出片段中所有参与对话的**真实人物角色**。
2. 如果是第一人称（“我”）叙述，请务必从文中寻找线索推断出主角的真实姓名。
3. 仔细对比【当前已知角色】，只有当提取出的角色**绝对不属于**已知角色时，才将其加入 `new_characters`。

#  绝对禁止的提取行为
-  **严禁拆分多重称呼（实体同一性）**：如果文中同时出现了全名、姓氏、外号或头衔（例如“常盘孤太郎”、“常盘”、“番长”），必须敏锐地意识到他们是同一个人！请将他们**合并**，统一使用最完整的全名（如“常盘孤太郎”）作为 `name`。如果该角色已经是【已知角色】，绝对不能因为叫了外号就当成新角色提取！
-  **严禁提取“虚构/被提及”角色（物理出场原则）**：只能提取在当前场景中**真实存在、有肉身物理出场并开口说话**的人。绝对禁止提取：
  - 仅在对话、回忆、传说中被别人提到的人。
  - 作中作、电子游戏、虚拟现实中的角色或怪物（例如游戏中的“士兵”“NPC”等）。
-  **严禁提取背景板**：绝对不要提取“旁白”、“作者”、“群众”、“店员”等没有明确私人名字且无足轻重的路人角色。

# 人物性格标签（仅可从下列标签中选择）
{allowed_character_tags}

# 本书个性化补充要求（仅适用于当前项目）
{project_pass1_prompt}

# 输出约束
- `gender` 仅可填写 `male` / `female` / `unknown`。
- `voice_archetype` 必须且只能填写上面列表中的一个标签，只有在人物个性鲜明时，分配对应标签，否则倾向于分配 `男-普通` 或 `女-普通`。
- 如果无法判断，`voice_archetype` 填 `未知`，`gender` 填 `unknown`。

请以 JSON 格式输出，不要包含 markdown 标记：
{{
  "new_characters": [ {{ "name": "...", "gender": "...", "voice_archetype": "...", "description": "..." }} ]
}}
"""

# ==========================================
# Pass 2: 剧本生成 (Script Supervisor)
# ==========================================
PASS2_PROMPT_TEMPLATE = """
你是一位专业的有声书场记。请根据“上一段上下文参考”和“当前小说片段”，生成逐句的结构化剧本。

# 本场可用演员表 (Available Actors)
{available_characters_str}
（格式仅含角色名和性别）

# 当前处理进度
第 {chunk_index}/{total_chunks} 段

# 上一段上下文参考 (仅用于衔接，不可直接复述)
{previous_chunk_context_str}

# 本书个性化补充要求（仅适用于当前项目）
{project_pass2_prompt}

# 任务目标
1. **彻底拆分**：将“当前小说片段”拆分为逐句剧本。如果一行包含“描述”和“对话”，必须拆分为多条。
# 2. **分配台词**：
   - 所有的对话（「」或“”内的内容）和明确的心理活动，`speaker` 必须严格从“可用演员表”中选择对应的角色名。
   - 小说为第一人称视角，所有非对话的旁白、内心独白等，speaker均标注为这一段第一人称视角的角色名字。
3. **上下文衔接**：利用“上一段上下文参考”识别代词、称呼和事件延续，避免误判说话者。
4. **清洗文本**：保留 `text` 字段中的引号和括号（如「」、“”），保留逗号句号用于停顿。

# 严格约束
- 严禁在 `speaker` 中捏造不存在于“演员表”中的名字。
- 只输出“当前小说片段”的内容，严禁重复输出“上一段上下文参考”里的句子。

请以 JSON 格式输出，不要包含 markdown 标记：
{{
  "script": [ {{ "text": "...", "speaker": "...", "emotion": "..." }} ]
}}
"""
